{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npm run build && node server.ts",
    "restartPolicyMaxRetries": 5
  }
}
