node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Ad-hoc API probe scripts. These tend to get written with credentials pasted
# inline while debugging a provider — keep them out of the repo entirely.
test-*.mjs
scratch-*.mjs
assets/
*.db-shm
*.db-walseed-docs.sql

# BOM snapshots taken by scripts/backup-bom-tables.mjs — local rollback
# artifacts, not source code. Keep out of git; hold onto them on disk
# for as long as the corresponding change window is still live.
backups/
